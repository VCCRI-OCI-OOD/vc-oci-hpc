---
ssl_cert_path: "/etc/ssl/certs"
ssl_ca_cert: "{{ ssl_cert_path }}/cluster-ca.crt"
ssl_cert_group: ssl
thirdparty_root_path: "/config/3rdparty"
thirdparty_arch_path: "{{ thirdparty_root_path }}/{{ ansible_facts['architecture'] }}"
binary_arch_map:
  x86_64: amd64
  aarch64: arm64
venv_path: "{% if ansible_facts['distribution'] == 'Ubuntu' %}/config/venv/{{ ansible_facts['distribution'] }}_{{ ansible_facts['distribution_version'] }}_{{ ansible_facts['architecture'] }}/oci{% elif
  ansible_facts['distribution'] == 'OracleLinux' %}/config/venv/Ol_{{ ansible_facts['distribution_version'] }}_{{ ansible_facts['architecture'] }}/oci{% endif %}"

# Identity provider configuration (populated from Terraform)
# Keep plain defaults here so the inventory can override them without creating
# a Jinja self-reference loop.
identity_provider: "freeipa"
# Convenience flags consumed by tasks/identity_client.yml and the OpenLDAP roles.
identity_freeipa_enabled: "{{ identity_provider == 'freeipa' }}"
identity_openldap_enabled: "{{ identity_provider == 'openldap' and (ldap | default(true) | bool) }}"
freeipa_server: ""
freeipa_realm: ""
freeipa_domain: ""
freeipa_enroll_principal: ""
freeipa_enroll_password: ""
freeipa_ca_cert_content: ""

